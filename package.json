{
  "name": "preact-start-monorepo",
  "version": "0.0.1",
  "author": "Konrad Miekus <miekus.konrad@gmail.com>",
  "packageManager": "pnpm@9.5.0",
  "private": true,
  "scripts": {
    "examples:counter": "pnpm config set link-workspace-packages false && pnpm install && pnpm --filter \"counter\" dev",
    "examples:counter:dev": "pnpm config set link-workspace-packages true && pnpm install && turbo run examples:counter:dev",
    "examples:blog": "pnpm config set link-workspace-packages false && pnpm install && pnpm --filter \"blog\" dev",
    "examples:blog:dev": "pnpm config set link-workspace-packages true && pnpm install && turbo run examples:blog:dev",
    "examples:api-server-todos": "pnpm config set link-workspace-packages false && pnpm install && pnpm --filter \"api-server-todos\" dev",
    "examples:api-server-todos:dev": "pnpm config set link-workspace-packages true && pnpm install && turbo run examples:api-server-todos:dev",
    "examples:server-actions-todos": "pnpm config set link-workspace-packages false && pnpm install && pnpm --filter \"server-actions-todos\" dev",
    "examples:server-actions-todos:dev": "pnpm config set link-workspace-packages true && pnpm install && turbo run examples:server-actions-todos:dev"
  },
  "devDependencies": {
    "turbo": "^2.0.7"
  }
}
