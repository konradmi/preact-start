{
  "name": "preact-start-monorepo",
  "version": "0.0.1",
  "author": "Konrad Miekus <miekus.konrad@gmail.com>",
  "private": true,
  "scripts": {
    "build": "pnpm --filter preact-start build",
    "examples:counter": "pnpm --filter counter dev"
  },
  "devDependencies": {
    "preact-start": "workspace:*"
  }
}
